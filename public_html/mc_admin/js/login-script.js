/* JS для шаблона входа (login)
------------------------------------------------------------------------------*/

$(window).load(function() {
    formSubmitOperaHack();
    
    $('#login').focus();
})
